apiVersion: apps/v1
kind: Deployment
metadata:
  name: account-deployment
  namespace: aline
  labels:
    aline.component: server
spec:
  replicas: 1
  selector:
    matchLabels:
      aline.component: server
  template:
    metadata:
      labels:
        aline.component: server
    spec:
      imagePullSecrets:
        - name: aws-registry-key
      containers:
      - name: bank
        image: 052911266688.dkr.ecr.us-west-1.amazonaws.com/aline-account-microservice
        ports:
          - containerPort: 8072
        env:
        - name: APP_PORT
          value: "8072"
        envFrom:
        - secretRef:
            name: aline-config
        resources:
          limits:
            cpu: "500m"
            memory: "1Gi"
          requests:
            cpu: "200m"
            memory: "512Mi"
      restartPolicy: Always
