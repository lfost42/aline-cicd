apiVersion: apps/v1
kind: Deployment
metadata:
  name: aline-user
  annotations:
    cluster-name: aline-financial
spec:
  selector:
    matchLabels:
      app: aline-user
  template:
    metadata:
      labels:
        app: aline-user
    spec:
      containers:
      - name: aline-user
        image: lyndasm/aline-user-microservice
        resources:
          requests:
            memory: "32Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 8070
