auth:
  database: alinedb
  username: user
  # password: ""
  # rootPassword: ""

namespaceOverride: aline
# helm repo add my-repo https://charts.bitnami.com/bitnami
# helm install mysql my-repo/mysql --values mysql-bn.yaml