auth:
  rootPassword: really_good_password
  database: alinedb
  username: user
  password: really_good_password
  
namespaceOverride: aline

# helm repo add my-repo https://charts.bitnami.com/bitnami
# helm install mysql my-repo/mysql --values manifest/mysql-bn.yaml