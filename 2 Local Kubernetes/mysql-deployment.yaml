apiVersion: apps/v1
kind: Deployment
metadata:
  name: aline-mysql
  annotations:
    cluster-name: aline-financial
spec:
  selector:
    matchLabels:
      app: aline-mysql
  template:
    metadata:
      labels:
        app: aline-mysql
    spec:
      containers:
      - name: aline-mysql
        image: lyndasm/aline-mysql
        resources:
          requests:
            memory: "32Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 3306
