apiVersion: apps/v1
kind: Deployment
metadata:
  name: member-deployment
  namespace: aline
  labels:
    aline.component: member
spec:
  replicas: 1
  selector:
    matchLabels:
      aline.component: member
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        aline.component: member
    spec:
      containers:
      - name: member
        image: lyndasm/aline-member
        ports:
          - containerPort: 80
        env:
          - name: REACT_APP_API_BASEURL
            value: http://api.alinefinancial.com
          - name: REACT_APP_API
            value: http://api.alinefinancial.com/api/
          - name: REACT_APP_MEMBER_DASHBOARD_URL
            value: http://member.alinefinancial.com
        resources:
          limits:
            cpu: "500m"
            memory: "1Gi"
          requests:
            cpu: "200m"
            memory: "512Mi"
      restartPolicy: Always
