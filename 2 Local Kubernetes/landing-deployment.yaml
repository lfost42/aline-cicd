apiVersion: apps/v1
kind: Deployment
metadata:
  name: aline-landing
  annotations:
    cluster-name: aline-financial
spec:
  selector:
    matchLabels:
      app: aline-landing
  template:
    metadata:
      labels:
        app: aline-landing
    spec:
      containers:
      - name: aline-landing
        image: lyndasm/aline-landing
        resources:
          requests:
            memory: "32Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 4200
