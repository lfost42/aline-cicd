apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    aline.service: admin
  name: admin
spec:
  replicas: 1
  selector:
    matchLabels:
      aline.service: admin
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        aline.service: admin
    spec:
      containers:
      - name: admin
        image: lyndasm/aline-admin
        ports:
          - containerPort: 3000
        resources:
          limits:
            cpu: "500m"
            memory: "1Gi"
          requests:
            cpu: "200m"
            memory: "512Mi"
      restartPolicy: Always

