apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-deployment
  namespace: aline
  labels:
    aline.component: admin
spec:
  replicas: 1
  selector:
    matchLabels:
      aline.component: admin
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        aline.component: admin
    spec:
      containers:
      - name: admin
        image: lyndasm/aline-admin
        ports:
          - containerPort: 3000
        env:
          - name: REACT_APP_API_BASEURL
            value: http://api.alinefinancial.com
          - name: REACT_APP_API
            value: http://api.alinefinancial.com/api/
        resources:
          limits:
            cpu: "500m"
            memory: "1Gi"
          requests:
            cpu: "200m"
            memory: "512Mi"
      restartPolicy: Always

