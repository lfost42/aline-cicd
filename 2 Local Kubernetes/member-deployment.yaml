apiVersion: apps/v1
kind: Deployment
metadata:
  name: aline-member
  annotations:
    cluster-name: aline-financial
spec:
  selector:
    matchLabels:
      app: aline-member
  template:
    metadata:
      labels:
        app: aline-member
    spec:
      containers:
      - name: aline-member
        image: lyndasm/aline-member
        resources:
          requests:
            memory: "32Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 3007
